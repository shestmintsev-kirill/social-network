import { UsersInitialStateType } from './users-reducer';

test('', () => {
    const state: UsersInitialStateType = {
        users: [],
        pageSize: 5,
        totalUsersCount: 0,
        currentPage: 1,
        isFetching: false,
        followingInProgress: [],
    };

    // @ts-ignore

    expect();
});
